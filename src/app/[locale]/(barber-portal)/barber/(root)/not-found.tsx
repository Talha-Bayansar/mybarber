import { Main } from "~/components/layout/main";
import { Title } from "~/components/layout/title";

const BarberNotFound = () => {
  return (
    <Main>
      <Title>You are not a barber.</Title>
    </Main>
  );
};

export default BarberNotFound;
